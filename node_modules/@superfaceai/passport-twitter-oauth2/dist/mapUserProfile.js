"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mapUserProfile = void 0;
const mapUserProfile = (json) => {
    let parsedJson;
    if ('string' === typeof json) {
        parsedJson = JSON.parse(json);
    }
    else {
        parsedJson = json;
    }
    const photos = parsedJson.profile_image_url
        ? [{ value: parsedJson.profile_image_url }]
        : [];
    const profile = {
        provider: 'twitter',
        id: parsedJson.id,
        username: parsedJson.username,
        displayName: parsedJson.name,
        profileUrl: parsedJson.url,
        photos,
    };
    return profile;
};
exports.mapUserProfile = mapUserProfile;
//# sourceMappingURL=mapUserProfile.js.map